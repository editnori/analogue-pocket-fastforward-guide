--- a/pkg/gbc/Cores/budude2.GBC/interact.json
+++ b/pkg/gbc/Cores/budude2.GBC/interact.json
@@ -16,10 +16,10 @@
         "type": "check",
         "enabled": true,
         "persist": true,
-        "address":    "0xF1000000",
-        "mask":       "0xFFFFFFFD",
+        "address": "0xF1000000",
+        "mask": "0xFFFFFFFD",
         "defaultval": 0,
-        "value":      "0x00000002"
+        "value": "0x00000002"
       },
       {
         "name": "Original Colors",
@@ -27,10 +27,10 @@
         "type": "check",
         "enabled": true,
         "persist": true,
-        "address":    "0xF2000000",
-        "mask":       "0xFFFFFFFD",
+        "address": "0xF2000000",
+        "mask": "0xFFFFFFFD",
         "defaultval": "0x00000002",
-        "value":      "0x00000002"
+        "value": "0x00000002"
       },
       {
         "name": "Enable Rumble",
@@ -38,10 +38,10 @@
         "type": "check",
         "enabled": true,
         "persist": true,
-        "address":    "0xF2000000",
-        "mask":       "0xFFFFFFFE",
+        "address": "0xF2000000",
+        "mask": "0xFFFFFFFE",
         "defaultval": "0x00000001",
-        "value":      "0x00000001"
+        "value": "0x00000001"
       },
       {
         "name": "Enable FF",
@@ -49,10 +49,10 @@
         "type": "check",
         "enabled": true,
         "persist": true,
-        "address":    "0xF2000000",
-        "mask":       "0xFFFFFFF7",
+        "address": "0xF2000000",
+        "mask": "0xFFFFFFF7",
         "defaultval": "0x00000008",
-        "value":      "0x00000008"
+        "value": "0x00000008"
       },
       {
         "name": "Enable FF Sound",
@@ -60,22 +60,53 @@
         "type": "check",
         "enabled": true,
         "persist": true,
-        "address":    "0xF2000000",
+        "address": "0xF2000000",
         "defaultval": 0,
-        "mask":       "0xFFFFFFFB",
-        "value":      "0x00000004"
+        "mask": "0xFFFFFFFB",
+        "value": "0x00000004"
       },
       {
-
         "name": "No Audio Pops",
         "id": 1006,
         "type": "check",
         "enabled": true,
         "persist": true,
-        "address":    "0xF2000000",
-        "mask":       "0xFFFFFF7F",
+        "address": "0xF2000000",
+        "mask": "0xFFFFFF7F",
         "defaultval": "0x00000000",
-        "value":      "0x00000080"
+        "value": "0x00000080"
+      },
+      {
+        "name": "Speed Level",
+        "id": 1010,
+        "type": "list",
+        "enabled": true,
+        "address": "0xF3000000",
+        "persist": true,
+        "writeonly": true,
+        "defaultval": 100,
+        "options": [
+          {
+            "name": "1x",
+            "value": 100
+          },
+          {
+            "name": "1.5x",
+            "value": 150
+          },
+          {
+            "name": "2x",
+            "value": 200
+          },
+          {
+            "name": "3x",
+            "value": 300
+          },
+          {
+            "name": "4x",
+            "value": 400
+          }
+        ]
       }
     ],
     "messages": []
--- a/src/core/core_top.sv
+++ b/src/core/core_top.sv
@@ -468,12 +468,14 @@
     32'hF8xxxxxx: begin bridge_rd_data = cmd_bridge_rd_data;          end
     32'hF1000000: begin bridge_rd_data = int_bridge_read_data;        end
     32'hF2000000: begin bridge_rd_data = int_bridge_read_data;        end
+    32'hF3000000: begin bridge_rd_data = int_bridge_read_data;        end
     default:      begin bridge_rd_data = 0;                           end
   endcase
 end
 
 reg [31:0] boot_settings = 32'h0;
 reg [31:0] run_settings  = 32'h0;
+reg [31:0] speed_settings = 32'd100;
 logic [31:0] int_bridge_read_data;
 
 always_ff @(posedge clk_74a) begin
@@ -484,6 +486,7 @@
       32'hF0000000: begin /*         RESET ONLY          */ reset_timer <= 1; end //! Reset Core Command
       32'hF1000000: begin boot_settings  <= bridge_wr_data; reset_timer <= 1; end //! System Settings
       32'hF2000000: begin run_settings   <= bridge_wr_data;                   end //! Runtime settings
+      32'hF3000000: begin speed_settings <= bridge_wr_data;                   end //! Speed percent
     endcase
   end
 
@@ -491,6 +494,7 @@
     case (bridge_addr)
       32'hF1000000: begin int_bridge_read_data  <= boot_settings;  end //! System Settings
       32'hF2000000: begin int_bridge_read_data  <= run_settings;   end //! Runtime settings
+      32'hF3000000: begin int_bridge_read_data  <= speed_settings; end //! Speed percent
     endcase
   end
 end
@@ -498,7 +502,7 @@
 logic clk_sys, clk_ram, clk_ram_90, clk_vid, clk_vid_90;
 logic pll_core_locked, pll_core_locked_s, reset_n_s, external_reset_s;
 logic [31:0] cont1_key_s, cont2_key_s, cont3_key_s, cont4_key_s;
-logic [31:0] boot_settings_s, run_settings_s;
+logic [31:0] boot_settings_s, run_settings_s, speed_settings_s;
 
 synch_3               s01 (pll_core_locked, pll_core_locked_s,  clk_ram);
 synch_3               s02 (reset_n,         reset_n_s,          clk_sys);
@@ -509,9 +513,11 @@
 synch_3 #(.WIDTH(32)) s07 (cont4_key,       cont4_key_s,        clk_sys);
 synch_3 #(.WIDTH(32)) s08 (boot_settings,   boot_settings_s,    clk_sys);
 synch_3 #(.WIDTH(32)) s09 (run_settings,    run_settings_s,     clk_sys);
+synch_3 #(.WIDTH(32)) s11 (speed_settings,  speed_settings_s,   clk_sys);
 synch_3               s10 (osnotify_adapter_play, cart_physical_mode, clk_sys);
 
 logic sgb_en, rumble_en, originalcolors, ff_snd_en, ff_en, sgb_border_en, gba_en, audio_no_pops;
+logic [9:0] ff_speed_percent;
 logic [1:0] tint;
 
 always_comb begin
@@ -527,6 +533,8 @@
   sgb_border_en  = run_settings_s[4];
   tint           = run_settings_s[6:5];
   audio_no_pops  = run_settings_s[7];
+  ff_speed_percent = (speed_settings_s[9:0] < 10'd100) ? 10'd100 :
+                     (speed_settings_s[9:0] > 10'd400) ? 10'd400 : speed_settings_s[9:0];
 end
 
 mf_pllbase mp1
@@ -1293,6 +1301,7 @@
   .speedup     ( fast_forward         ),
   .cart_act    ( cart_act             ),
   .DMA_on      ( DMA_on               ),
+  .speed_percent( ff_speed_percent    ),
   .ce          ( ce_cpu               ),
   .ce_2x       ( ce_cpu2x             ),
   .refresh     ( sdram_refresh_force  ),
--- a/src/gb/speedcontrol.vhd
+++ b/src/gb/speedcontrol.vhd
@@ -10,6 +10,7 @@
       speedup     : in     std_logic;
       cart_act    : in     std_logic;
       DMA_on      : in     std_logic;
+      speed_percent : in   unsigned(9 downto 0);
       ce          : out    std_logic := '0';
       ce_2x       : buffer std_logic := '0';
       refresh     : out    std_logic := '0';
@@ -25,6 +26,8 @@
                            
    signal unpause_cnt      : integer range 0 to 15 := 0;    
    signal fastforward_cnt  : integer range 0 to 15 := 0;    
+   signal ff_rate_accum    : integer range 0 to 799 := 0;
+   signal ff_rate_accum_2x : integer range 0 to 399 := 0;
    
    signal refreshcnt       : integer range 0 to 127 := 0;
    signal sdram_busy       : integer range 0 to 1 := 0;
@@ -65,6 +68,8 @@
                elsif (speedup = '1' and pause = '0' and DMA_on = '0' and clkdiv = "000") then
                   state           <= FASTFORWARDSTART;
                   fastforward_cnt <= 0;
+                  ff_rate_accum    <= 0;
+                  ff_rate_accum_2x <= 0;
                else
                   clkdiv <= clkdiv + 1;
                   if (clkdiv = "000") then
@@ -100,6 +105,8 @@
                if (pause = '1' or speedup = '0' or DMA_on = '1') then
                   state           <= FASTFORWARDEND;
                   fastforward_cnt <= 0;
+                  ff_rate_accum    <= 0;
+                  ff_rate_accum_2x <= 0;
                   if (clkdiv(0) = '1') then
                      clkdiv <= "100";
                   end if;
@@ -112,11 +119,18 @@
                   state      <= RAMACCESS;
                   sdram_busy <= 1;
                else
-                  clkdiv(0) <= not clkdiv(0);
-                  if (clkdiv(0) = '0') then
-                     ce      <= '1';
+                  if (ff_rate_accum + to_integer(speed_percent) >= 800) then
+                     ff_rate_accum <= ff_rate_accum + to_integer(speed_percent) - 800;
+                     ce           <= '1';
+                  else
+                     ff_rate_accum <= ff_rate_accum + to_integer(speed_percent);
                   end if;
-                  ce_2x   <= '1';
+                  if (ff_rate_accum_2x + to_integer(speed_percent) >= 400) then
+                     ff_rate_accum_2x <= ff_rate_accum_2x + to_integer(speed_percent) - 400;
+                     ce_2x            <= '1';
+                  else
+                     ff_rate_accum_2x <= ff_rate_accum_2x + to_integer(speed_percent);
+                  end if;
                end if;
             
             when FASTFORWARDEND =>
